[project]
name = "gipfelsturm"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
    "tabulate>=0.9",
    "matplotlib>=3.7",
]

[tool.pytest.ini_options]
testpaths = ["experiments"]
